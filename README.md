# Ty Studio

一个开箱即用的 Vue3 + Vite 前端基础框架，集成 Element Plus、Tailwind CSS、Pinia、Vue Router，并提供权限控制与请求封装的扩展结构。

## 特性
- 🔧 现代开发：Vite + Vue3 组合式 API
- 🎨 UI 集成：Element Plus + Tailwind（可扩展主题）
- 🧩 目录约定：组件 / 视图 / 请求 / 工具 / 配置 分层清晰
- 🔐 权限机制：基于路由 meta 与本地存储（可扩展到角色与资源粒度）
- 🚀 可扩展：支持插件化注入工具、请求层抽象、动态菜单

## 快速开始
```bash
pnpm install
pnpm dev
```

## 目录结构
```
src/
  main.js
  App.vue
  router/
  stores/
  request/
  utils/
  config/
  views/
  components/
  assets/
```

## 环境变量
在 `.env.development` / `.env.production` 中配置：
```
VITE_API_BASE=https://api.example.com
```

## 脚本
| 命令 | 说明 |
| ---- | ---- |
| pnpm dev | 开发环境启动 |
| pnpm build | 构建生产包 |
| pnpm preview | 本地预览生产包 |
| pnpm lint | 代码规范检查 |

## 后续规划
- [ ] 引入测试（Vitest）
- [ ] 插件体系（自动注册核心服务）
- [ ] 动态菜单与权限升级
- [ ] TypeScript 支持
- [ ] CI/CD & 规范提交

## License
MIT